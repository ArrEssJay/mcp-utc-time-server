# MCP UTC Time Server - Environment Configuration
# Copy this file to .env and configure for your environment
# DO NOT commit .env to version control!

# =============================================================================
# MCPO Configuration (HTTP Wrapper)
# =============================================================================

# API Key for authentication (REQUIRED for MCPO)
# Generate with: openssl rand -base64 32
MCPO_API_KEY=your-secure-api-key-here

# HTTP server port (default: 8000)
MCPO_PORT=8000

# Enable MCPO debug output (optional)
# MCPO_DEBUG=1

# =============================================================================
# Logging Configuration
# =============================================================================

# Rust log level: trace, debug, info, warn, error
RUST_LOG=info

# Log to file (optional)
# RUST_LOG_FILE=mcp-server.log

# =============================================================================
# Timezone Configuration
# =============================================================================

# System timezone (default: UTC, recommended)
TZ=UTC

# Alternative timezone examples:
# TZ=America/New_York
# TZ=Europe/London
# TZ=Asia/Tokyo

# =============================================================================
# Cloudflare Tunnel Configuration (Optional)
# =============================================================================

# These are automatically managed by start_tunnel.sh
# You can set them manually if needed

# Tunnel URL (auto-generated by cloudflared)
# TUNNEL_URL=https://xxx.trycloudflare.com

# Cloudflare account token (for persistent tunnels)
# CF_TUNNEL_TOKEN=your-tunnel-token

# =============================================================================
# VSCode Configuration (Optional)
# =============================================================================

# Path to binary for VSCode settings.json
# MCP_BINARY_PATH=/absolute/path/to/target/release/mcp-utc-time-server

# VSCode workspace folder
# MCP_WORKSPACE=/path/to/mcp-utc-time-server

# =============================================================================
# Development Configuration
# =============================================================================

# Enable backtrace for debugging
# RUST_BACKTRACE=1
# RUST_BACKTRACE=full

# Cargo build optimization
# CARGO_PROFILE_RELEASE_LTO=true
# CARGO_PROFILE_RELEASE_OPT_LEVEL=3

# =============================================================================
# Performance Tuning (Advanced)
# =============================================================================

# Tokio runtime threads (default: auto-detect)
# TOKIO_WORKER_THREADS=4

# =============================================================================
# Usage Examples
# =============================================================================

# Load this file:
#   source .env
#   # or
#   export $(cat .env | grep -v '^#' | xargs)

# Quick start with tunnel:
#   export MCPO_API_KEY="$(openssl rand -base64 32)"
#   ./scripts/start_tunnel.sh

# Local MCPO only:
#   export MCPO_API_KEY="your-key"
#   ./scripts/start_mcpo.sh

# VSCode integration:
#   No .env needed - configure in .vscode/settings.json
